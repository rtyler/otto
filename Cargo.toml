[workspace]

members = [
    "agent",

    "osp",

    "services/auctioneer",
    "services/eventbus",
    "services/object-store",

    "stdlib/archive",
    "stdlib/dir",
    "stdlib/error",
    "stdlib/sh",
]

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
incremental = false
opt-level = "z"
